.onAttach <- function(libname, pkgname){
  # hash the opening book

}



assign_hash <- Vectorize(assign, vectorize.args = c("x", "value"))
get_hash <- Vectorize(get, vectorize.args = "x")
exists_hash <- Vectorize(exists, vectorize.args = "x")

# initialize hash
h <- new.env(hash = TRUE, size = length(out))

library(hash)

keys <- names(out[1])

values <- out[[1]]

assign_hash(x = keys, value = values, envir = h)
out[[1]]$
  h[["rnbakabnr/9/1c5c1/p1p1p1p1p/9/6P2/P1P1P3P/1C5C1/9/RNBAKABNR%20b"]]
get_hash("rnbakabnr/9/1c5c1/p1p1p1p1p/9/6P2/P1P1P3P/1C5C1/9/RNBAKABNR%20b", envir = h)

h <- hash(names(out)[1], out[[1]])

h[["rnbakabnr/9/1c5c1/p1p1p1p1p/9/6P2/P1P1P3P/1C5C1/9/RNBAKABNR%20b"]][["h7h0"]]
